import 'package:mca_new_design/template/base/template.dart';

class AvailableShiftScreen extends StatelessWidget {
  const AvailableShiftScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
